POSTGRES_DB=library_db
POSTGRES_USER=dit_user
POSTGRES_PASSWORD=dit_password

# Optionnel — origines navigateur pour CORS (Vite utilise souvent :5173 ; un 2ᵉ projet peut passer sur :5174).
# Une entrée par service FastAPI peut être surchargée en redéfinissant cette variable puis reconstruire les images.
# CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174
