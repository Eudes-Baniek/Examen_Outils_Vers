---
description: Agent Backend — fichiers YAML DVC (dvc.yaml, métadonnées). Karpathy + cohérence pipeline.
globs: ml/**/*.yaml
alwaysApply: false
---

# Agent Backend — config DVC (`ml/`)

Respecter **karpathy-guidelines**.

- Modifier `dvc.yaml` **chirurgicalement** ; chaque étape doit avoir `deps` / `outs` / métriques explicites.
- Après changement pipeline, prévoir **`dvc repro`** et mise à jour de `dvc.lock`.

Doc officielle **DVC** (DAG, parametrization, metrics).
